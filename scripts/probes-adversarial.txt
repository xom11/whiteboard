# === PHẢI render đúng (13 ca tốt — không được regress) ===
Cho tam giác ABC. Gọi M là trung điểm BC
Cho tam giác ABC. Gọi G là trọng tâm tam giác ABC
Cho tam giác ABC. Kẻ đường cao AH
Cho tam giác ABC. Gọi H là hình chiếu của A trên BC
Cho hình vuông ABCD
Cho hình chữ nhật ABCD
Cho tam giác ABC vuông tại A. Gọi M là trung điểm BC
Cho tam giác ABC cân tại A. Kẻ trung tuyến AM
Cho đường tròn tâm O bán kính 3
Cho đường tròn (O; 3)
Cho tam giác ABC. Vẽ đường tròn ngoại tiếp tam giác ABC
Cho tam giác ABC. Vẽ đường tròn nội tiếp tam giác ABC
Cho tam giác ABC. Gọi H là trực tâm của tam giác ABC

# === PHẢI escalate (HIGH: hình sai im lặng nếu render) ===
Cho tam giác ABC và tam giác ABD vuông tại A
Cho tam giác ABC và tam giác ACD cân tại A
Cho tam giác đều DEF nội tiếp tam giác ABC
Cho tam giác ABC nội tiếp tam giác đều MNP
Cho hình bình hành ABCD và hình chữ nhật EFGH
Cho hình thoi ABCD và hình vuông EFGH
Cho tam giác ABC và tam giác DEF. Gọi G là trọng tâm tam giác DEF
Cho tam giác ABC. Lấy tam giác MBC. Gọi H là trực tâm tam giác MBC
Cho tam giác ABC. Dựng tam giác DEF. Gọi I là tâm đường tròn nội tiếp tam giác DEF
Cho tam giác ABC. Kẻ đường cao AB
Cho tam giác ABC. Vẽ trung tuyến AC
Cho tam giác ABC. B đối xứng C qua M
Cho tam giác MNP. M đối xứng N qua P
Cho tam giác ABC. A đối xứng B qua AC
Cho tam giác ABC. Gọi M là trung điểm BC và N là trung điểm AC
Cho tam giác ABC. Vẽ đường trung trực BC và trung trực CA
Cho tam giác ABC. Gọi N là điểm bất kỳ, H là hình chiếu của A trên BC
Cho tam giác ABC. Gọi H, K lần lượt là hình chiếu của B trên AC và của C trên AB
Cho tam giác ABC. Vẽ đường tròn ngoại tiếp tứ giác BCEF
Cho đường tròn (O; 3). Độ dài tiếp tuyến từ A đến (O) bằng 5
Cho tam giác ABC. Kéo dài AB về phía B lấy điểm D sao cho BD = 2 AB.
Cho tam giác ABC nội tiếp đường tròn (O; R). Kéo dài AB về phía B lấy điểm D sao cho BD = 2R.
Cho tam giác ABC nội tiếp đường tròn (O; R). Kéo dài AB về phía B lấy điểm D sao cho BD = 2·R.
Cho tam giác ABC nội tiếp đường tròn (O; R). Trên tia đối của tia BA lấy D sao cho BD = R + 1.
Cho tam giác ABC. Kéo dài AB về phía A lấy điểm D sao cho BD = 3.
Cho tam giác ABC nội tiếp đường tròn (O). Gọi M là điểm chính giữa cung BC không chứa B
Cho đường tròn tâm I bán kính 2 và đường tròn tâm O bán kính 5
Vẽ đoạn thẳng song song với BC

# === Mức 2: PHẢI render (vá coverage gap — verify end-to-end) ===
Cho tam giác ABC vuông tại A. Gọi M là trung điểm cạnh huyền BC
Cho tam giác ABC. Gọi I là trung điểm đoạn thẳng AC
Cho tam giác ABC. Vẽ phân giác trong AD
Cho tam giác ABC. Dựng đường phân giác trong AD
Cho tam giác ABC. Vẽ phân giác ngoài AD
Cho tam giác ABC. Kẻ phân giác ngoài AD
Cho tam giác ABC. Kẻ AH vuông góc BC tại H
Cho tam giác ABC. Kẻ AH ⊥ BC tại H
Đường tròn (O; R) ngoại tiếp tam giác ABC
Cho tam giác ABC ngoại tiếp đường tròn (I)
Cho tam giác MNP ngoại tiếp đường tròn tâm O
Cho tam giác ABC. Trên tia đối của tia BA lấy điểm D′ sao cho BD′ = AB
Cho tam giác ABC. Vẽ tia phân giác của góc BAC
Cho tam giác ABC. Vẽ phân giác góc A

# === Mức 2: PHẢI escalate (fail-safe — construct chưa hỗ trợ / xung đột) ===
Cho tam giác ABC. Kẻ AH vuông góc BC tại K
Cho tam giác ABC. M là trung điểm cạnh thẳng BC
Cho tam giác ABC. Vẽ phân giác ngoài AB

# === Issue #46 nhóm A: "(O; R)" bán kính ký hiệu CHỮ — PHẢI render (minh hoạ) ===
Cho đường tròn (O; R)
Cho đường tròn tâm O bán kính R

# === Issue #46: PHẢI escalate (hệ số bán kính defer Cụm C) ===
Cho đường tròn (O; 2R)

# === Issue #46 nhóm A: "⊥" standalone — PHẢI render (≡ "vuông góc") ===
# Clause perpendicular dùng "Vẽ/Dựng" (KHÔNG phải connect-keyword "Kẻ") nên
# perpFoot là rule DUY NHẤT claim. Sau fix, "⊥" là tín hiệu hình học
# (hasGeometry) ⇒ clause vào denominator coverage VÀ được perpFoot claim ⇒
# complete (covered=[base, ⊥-clause]). Render perpFoot, ĐÚNG như phrasing
# "vuông góc". Verify clause "⊥" nằm trong coverage denominator (không bị coi
# là văn xuôi rồi bỏ qua im lặng).
Cho tam giác ABC. Vẽ AH ⊥ BC tại H
Cho tam giác ABC. Dựng BK ⊥ AC tại K

# === Issue #46 nhóm A: "⊥" standalone — PHẢI escalate (fail-safe) ===
# "⊥" là tín hiệu hình học nhưng KHÔNG rule nào claim cụm "AH ⊥ BC" (thiếu
# verb Kẻ/Vẽ/Dựng). Sau fix hasGeometry=true ⇒ clause vào denominator ⇒
# uncovered ⇒ escalate ở tầng coverage (trước fix escalate muộn hơn ở
# named-entity guard). Hai phía đều escalate-safe, không render thiếu quan hệ.
Cho tam giác ABC. Biết AH ⊥ BC tại H

# === Issue #46 nhóm A: "tia đối" connect — PHẢI render ===
# "tia đối của tia BA" = tia gốc B đi NGƯỢC hướng A; điểm mới (D) nằm trên tia
# ĐỐI (xa A). Trước fix connect.ts khớp "tia BA" bên trong cụm → emit ray B→A
# (hướng tia GỐC, SAI hướng so với D) = silent-wrong bug. Sau fix connect.ts
# suppress ray naive này; pointAtDistance dựng D đúng (from=A, through=B ⇒ D
# nằm ngoài B, xa A). Render với add-point/pointAtDistance, KHÔNG còn connect/ray
# sai hướng. (Verify hướng: kiểm intents không chứa connect ray B→A.)
Cho tam giác ABC. Trên tia đối của tia BA lấy điểm D sao cho BD = AB
# "tia AB" trần (KHÔNG "đối") vẫn render ray A→B đúng hướng — không phá case thường.
Cho tam giác ABC. Vẽ tia AB

# === Issue #46 nhóm A: "tia đối" connect — escalate-safe ===
# "tia đối của tia BA" nhưng KHÔNG có cụm "lấy điểm … sao cho … = …" hợp lệ để
# pointAtDistance dựng điểm mới (thiếu mốc khoảng cách). connect.ts suppress ray
# sai hướng (không render thừa), không rule nào claim cụm → escalate (fail-safe,
# không vẽ ray sai hướng cũng không bịa điểm).
Cho tam giác ABC. Trên tia đối của tia BA lấy điểm D

# === Issue #46 nhóm A: prime "C′" trùng đỉnh — PHẢI render ===
# Tam giác ABC rồi "lấy điểm C′" (trùng CHỮ đỉnh C). Trước fix naming layer STRIP
# prime → "C" → trùng đỉnh C → addPoint dedup drop → guard intent-dropped → escalate.
# Sau fix: prime được GIỮ + normalize ′(U+2032)→'(U+0027) ⇒ "C'" là tên RIÊNG,
# khác đỉnh "C". DSL có CẢ điểm C (free vertex) LẪN C' (pointAtDistance). Verify:
# add-point/pointAtDistance name="C'", KHÔNG escalate, KHÔNG drop.
Cho tam giác ABC. Trên tia đối của tia BA lấy điểm C′ sao cho BC′ = AB
# Apostrophe ASCII (') cũng giữ nguyên (cùng đường normalize) — name "C'".
# Dùng "= AB" (segmentLength, không cần circle) để cô lập đúng fix prime-naming.
Cho tam giác ABC. Trên tia đối của tia BA lấy điểm C' sao cho BC' = AB

# === Issue #46 nhóm A: prime ở construct CHƯA hỗ trợ — PHẢI escalate (fail-safe) ===
# Prime "C′" trên construct KHÔNG phải pointAtDistance (đặt điểm tự do không mốc/
# khoảng cách hợp lệ) → không rule nào claim cụm prime → escalate, không bịa điểm.
Cho tam giác ABC. Lấy điểm C′ bất kỳ

# === Issue #46 nhóm C: tứ giác nội tiếp đường tròn (cyclic quadrilateral) ===
# Render-correct: 4 đỉnh đồng viên + đường tròn ngoại tiếp circle3 qua cả 4.
Cho tứ giác ABCD nội tiếp đường tròn (O)
Đường tròn (O) ngoại tiếp tứ giác MNPQ

# === EN render (issue #46 group B) ===
# Tier 0→1 tiếng Anh: triangle/quad/circle/midpoint + variant. Tất cả PHẢI DET OK
# và tương đương bản tiếng Việt (cùng ops/shapes/variants/constraints).
Triangle ABC
Right triangle ABC, right angle at A
Square ABCD
Circle (O; 3)
Circle with center O and radius 3
Triangle ABC. M is the midpoint of BC.

# === EN escalate-safe (issue #46 group B) ===
# (a) construct CHƯA hỗ trợ (radical axis) → escalate (no-match), không vẽ sai.
Draw the radical axis of two circles.
# (b) geo-clause không rule nào claim ("diameter"/"circle" → geo nhưng vô chủ) →
# escalate (incomplete-coverage), không render thiếu hình.
Triangle ABC. The diameter of the circle is AB.

# === EN centers (issue #46 group B) ===
# Tâm tam giác tiếng Anh (centroid/orthocenter/circumcenter/incenter). PHẢI DET OK,
# bind of=[A,B,C] qua tam giác trong clause hoặc fallback tam giác duy nhất.
Triangle ABC. G is the centroid of triangle ABC.
Triangle ABC. Let H be the orthocenter of triangle ABC.
# Escalate-safe: "centroid" là geo nhưng KHÔNG có tam giác nào để bind of →
# không claim → escalate (KHÔNG bịa tam giác).
G is the centroid.

# === EN perpBisector + reflection (issue #46 group B) ===
# Render-correct: triangle nền + construct EN.
Triangle ABC. Draw the perpendicular bisector of BC.
Triangle ABC. D is the reflection of A over BC.
# Escalate-safe: "bisector" là geo nhưng không có cặp đỉnh → escalate (không render sai).
Draw the perpendicular bisector of the triangle.

# === EN angleBisectorAngle (issue #46 group B) ===
# Render-correct: phân giác theo GÓC tiếng Anh trên tam giác nền.
# Form A 3-point (vertex = chữ giữa): "bisector of angle BAC" → bis BAC {B,A,C}.
Triangle ABC. Draw the bisector of angle BAC.
# Form A 1-letter ("angle A" → suy 2 cạnh B,C từ tam giác): bis BAC {B,A,C}.
Triangle ABC. Draw the bisector of angle A.
# Form B "angle bisector of XYZ" (vertex giữa). Cần tam giác cùng tên để anchor
# 3 đỉnh (3-point standalone không có tam giác → transpile UNKNOWN_REF, escalate).
Triangle MNP. Draw the angle bisector of MNP.
# Escalate-safe: "angle A" 1-letter KHÔNG có tam giác → không suy được 2 tia →
# escalate (no-match), KHÔNG render sai.
Draw the bisector of angle A.

# === EN tangentFromExt (issue #46 group B) ===
# Rule EN match đúng (emit drawLine tangentFromExt {from:A, circle:O, both}) — chứng
# minh qua transpile-fail "throughPoint A không tồn tại" (intent ĐÃ phát, chỉ thiếu
# điểm ngoài A). tangentFromExt KHÔNG đạt DET render dù VN hay EN: hiện KHÔNG rule
# nào dựng điểm NGOÀI A deterministic (gap có sẵn, language-agnostic). Escalate ở
# đây là FAIL-SAFE (không render thiếu A), KHÔNG phải lỗi mới của nhánh EN.
# Match-correct → escalate (transpile-fail A): "(O)" ký hiệu. TRƯỚC fix: clause
# này hasGeometry=false ("(O)" không text-keyword, chưa có vocab "tangent") →
# coi là văn xuôi → render circle+free-A BỎ tiếp tuyến (SILENT-INCOMPLETE). Vocab
# "tangent" + rule EN → clause thành geo + claim → escalate fail-safe đúng.
Circle (O; 3). Draw two tangents from A to (O).
# Match-correct → escalate (transpile-fail A): "circle O" dạng chữ.
Circle (O; 3). Draw two tangent lines from A to circle O.
# Escalate-safe: KHÔNG có draw/construct ("The length ... is 5") → no-match →
# escalate, KHÔNG render sai.
The length of the tangent from A to (O) is 5.

# === Điểm NGOÀI đường tròn + tiếp tuyến từ điểm ngoài (issue #46 nhóm C) ===
# Render-win: rule externalPoint (prio 68) dựng A free NGOÀI (O) (đọc tâm+bán kính
# từ build state, đặt A = center + [r*2,0]) → unblock tangentFromExt render END-TO-END.
# Khác L186/L188 (chỉ có mệnh đề tiếp tuyến, A không khai báo → escalate): các đề
# dưới có ĐỦ mệnh đề "Lấy điểm A ngoài (O)" → A dựng được → tiếp tuyến render.
# DET render: VN "nằm ngoài (O)" + "vẽ hai tiếp tuyến từ A đến (O)".
Cho đường tròn (O; 3). Lấy điểm A nằm ngoài (O). Vẽ hai tiếp tuyến từ A đến (O).
# DET render: VN "ở ngoài đường tròn (O)" + biến thể "Từ A kẻ ... đến (O)".
Cho đường tròn (O; 3). Lấy điểm A ở ngoài đường tròn (O). Từ A kẻ hai tiếp tuyến đến (O).
# DET render: EN "Take a point A outside (O)" + "Draw two tangents from A to (O)".
Circle (O; 3). Take a point A outside (O). Draw two tangents from A to (O).
# === Issue #46 named-tangent-point: đặt TÊN tiếp điểm "tiếp tuyến AB, AC" ===
# RENDER-WIN cả VN+EN (trước escalate named-missing — DEFER batch9): tiếp điểm B,C
# dựng qua tangentPointExt (from=A điểm ngoài, which 0/1) + 2 tiếp tuyến. Điểm
# ngoài A = chữ cái đầu CHUNG của 2 đoạn; circle phải khai báo có bán kính.
# DET render: VN "Vẽ hai tiếp tuyến AB, AC từ A đến (O)" → +B,C tangentPointExt.
Cho đường tròn (O; 3). Lấy điểm A nằm ngoài (O). Vẽ hai tiếp tuyến AB, AC từ A đến (O).
# DET render: VN implicit-from "và" + "(B, C là tiếp điểm)" → +B,C.
Cho đường tròn (O; 3). Lấy điểm A nằm ngoài (O). Kẻ hai tiếp tuyến AB và AC tới (O) (B, C là các tiếp điểm).
# DET render: VN inverted "Từ A kẻ hai tiếp tuyến AB, AC đến (O)" → +B,C.
Cho đường tròn (O; 3). Lấy điểm A ở ngoài đường tròn (O). Từ A kẻ hai tiếp tuyến AB, AC đến (O).
# DET render: EN explicit-from "Draw two tangents AB, AC from A to (O)" → +B,C.
Circle (O; 3). Take a point A outside (O). Draw two tangents AB, AC from A to (O).
# DET render: EN implicit-from "and" "Draw two tangents AB and AC to (O)" → +B,C.
Circle (O; 3). Take a point A outside (O). Draw two tangents AB and AC to (O).
# Escalate-safe: 2 đoạn KHÁC điểm ngoài ("AB, CD") → named không fire (A≠C),
# unnamed cũng không match ("AB, CD" trước circle) → escalate, KHÔNG render sai.
Cho đường tròn (O; 3). Lấy điểm A nằm ngoài (O). Vẽ hai tiếp tuyến AB, CD từ A đến (O).

# === EN perpFoot (issue #46 nhóm B Tier 2) ===
# Render-win EN: rule perpFoot +EN (projection / foot-of-perpendicular / draw-form).
# VN tương ứng đã render OK (hình chiếu / chân đường vuông góc / Kẻ AH ⊥ BC). EN
# parity: cùng add-point perpFoot H; draw-form tự emit connect(A,H) vì connect.ts
# VN-only. Vocab +perpendicular/projection → clause EN thành geo-clause (fail-safe).
# DET render: "projection of A onto BC" → perpFoot H (no segment).
Triangle ABC. Let H be the projection of A onto BC.
# DET render: "foot of the perpendicular from A to BC" → perpFoot H.
Triangle ABC. Let H be the foot of the perpendicular from A to BC.
# DET render: draw form "Draw AH perpendicular to BC at H" → perpFoot H + đoạn AH.
Triangle ABC. Draw AH perpendicular to BC at H.
# Escalate-safe: KHÔNG có "X be/is the" để bind tên chân → perpFoot không claim →
# clause geo ("perpendicular") không phủ → escalate (KHÔNG silent-incomplete).
Triangle ABC. The foot of the perpendicular from A meets BC.

# === EN circleTriangle (issue #46 nhóm B Tier 2) ===
# Render-win EN: rule circleTriangle +EN (inscribe/circumscribe). VN render OK.
# TÁC ĐỘNG: fix silent-incomplete (trước render triangle-only, rớt đường tròn) +
# fix silent-WRONG (case (O;3): trước vẽ circle radius-3 RỜI không qua A,B,C; nay
# circleRadius PAREN branch nhận "inscribed" EN qua isInscribedCircumscribed mirror
# → bỏ centerRadius rời → chỉ còn circle3 qua 3 đỉnh). Win lộ ở dòng OPS, KHÔNG count.
# DET render: "triangle inscribed in circle (O)" → through3 circumcircle.
Triangle ABC inscribed in circle (O).
# DET render: "circle (O) circumscribes triangle ABC" → through3.
Circle (O) circumscribes triangle ABC.
# DET render: "circle (I) inscribed in triangle ABC" → inscribedIn incircle.
Circle (I) inscribed in triangle ABC.
# DET render (silent-WRONG fix): "(O; 3)" inscribe → chỉ 1 circle3, KHÔNG circleCR rời.
Triangle ABC inscribed in circle (O; 3).
# Escalate-safe: "circumscribes the figure" KHÔNG có tam giác 3-đỉnh → circleTriangle
# không claim → clause geo ("circle") không phủ → escalate (KHÔNG render sai).
Circle (O) circumscribes the figure.

# === EN arcMidpoint (issue #46 nhóm B Tier 2) ===
# Render-win EN: rule arcMidpoint +EN ("midpoint of arc BC not containing A"). VN
# tương ứng render OK ("điểm chính giữa cung nhỏ BC không chứa A"). TÁC ĐỘNG: flip
# ESCALATE→DET thật — trước fix clause "midpoint of arc BC" có keyword "midpoint"
# (geo-clause) nhưng KHÔNG rule nào claim → incomplete-coverage escalate; nay
# arcMidpoint EN claim → render đủ M trên cung (parity VN). KHÔNG đụng vocabulary
# ("midpoint" đã có). Collision midpoint EN không xảy ra ("arc" chữ thường phá [A-Z][A-Z]).
# DET render: "not containing A" tường minh → arcMidpoint M (a=B,b=C,notContaining=A).
Triangle ABC is inscribed in circle (O). Let M be the midpoint of arc BC not containing A.
# DET render: name-before "is" + "the arc" → arcMidpoint M.
Triangle ABC is inscribed in circle (O). M is the midpoint of the arc BC not containing A.
# DET render: "minor arc" KHÔNG nêu "not containing" → suy đỉnh thứ 3 tam giác (A).
Triangle ABC is inscribed in circle (O). Let M be the midpoint of the minor arc BC.
# Escalate-safe: "major arc" → cung đối, defer (mirror VN "cung lớn") → arcMidpoint
# không claim → clause geo ("midpoint") không phủ → escalate (KHÔNG render sai).
Triangle ABC is inscribed in circle (O). Let M be the midpoint of the major arc BC.

# === EN cyclic-quad (tứ giác nội tiếp đường tròn) (issue #46 nhóm B Tier 2) ===
# FIX SILENT-INCOMPLETE EN: detectCyclic mở rộng sang EN (Pattern A "inscribed in
# circle (O)", Pattern B "circle (O) circumscribes"). VN render OK (quad concyclic
# + through3). Trước fix đề EN render quad-ONLY (rớt đường tròn âm thầm); nay parity
# VN. Win lộ ở DÒNG OPS (+draw-circle/through3), KHÔNG render count (đã render
# quad-only). KHÔNG đụng vocab ("quadrilateral"+"circle" đã có).
# DET render: Pattern A "is inscribed in circle (O)" → quad + through3.
Quadrilateral ABCD is inscribed in circle (O).
# DET render: Pattern A không "is" → quad + through3.
Quadrilateral ABCD inscribed in circle (O).
# DET render: Pattern B "Circle (O) circumscribes quadrilateral MNPQ" → quad + through3.
Circle (O) circumscribes quadrilateral MNPQ.
# Fail-safe quad-only: đỉnh ABCD chia A,B,C với "Triangle ABC" → ownedByOthers chặn
# concyclic (TRIANGLE_DECL nay nhận EN) → quad + triangle, KHÔNG circle (chống silent-wrong).
Triangle ABC. Quadrilateral ABCD is inscribed in circle (O).
# Escalate-safe: 5 đỉnh "ABCDE" → QUAD cần đúng 4 đỉnh → không quad hit → clause geo
# ("circle"/"quadrilateral") không phủ → escalate (KHÔNG render sai).
Circle (O) circumscribes quadrilateral ABCDE.

# === EN pointAtDistance (điểm trên tia/đoạn kéo dài, cách mốc khoảng d) (issue #46 nhóm B Tier 2) ===
# FIX SILENT-INCOMPLETE EN: rule pointAtDistance +EN (mirror VN tia đối/kéo dài).
# VN render OK ("Trên tia đối của tia BA lấy D sao cho BD = …"). Trước fix đề EN
# render TRIANGLE-ONLY — clause "On ray BA extended beyond A, take D such that
# AD = AB" thiếu vocab EN ("extend"/"opposite ray") → coi văn xuôi → coverage
# complete trên tam giác, âm thầm BỎ điểm D (silent-drop). Nay +vocab "extend"/
# "opposite ray" + EN pattern → clause geo + claim → render đủ D (parity VN).
# Win lộ ở DÒNG OPS (+add-point/pointAtDistance), KHÔNG render count.
# DET render: "ray BA extended beyond A" → from=B, through=A; AD=AB segmentLength.
Triangle ABC. On ray BA extended beyond A, take D such that AD = AB.
# DET render: "ray AB extended beyond B" → from=A, through=B; BD=5 literal.
Triangle ABC. On ray AB extended beyond B, take D such that BD = 5.
# DET render: "Extend AB beyond B to D" (đoạn, không "ray") → from=A, through=B.
Triangle ABC. Extend AB beyond B to D such that BD = AB.
# DET render: "opposite ray of ray BA" → mirror VN tia đối: from=A, through=B.
Triangle ABC. On the opposite ray of ray BA, take D such that BD = AB.
# DET render: distance "= R" (circleRadius) resolve circle O từ "(O; 3)"; cần A,B
# từ tam giác nội tiếp (circleTriangle EN) → triangle + through3 + D.
Triangle ABC inscribed in circle (O; 3). On ray AB extended beyond B, take D such that BD = R.
# Escalate-safe: thiếu cụm khoảng cách "= …" → pointAtDistance không dựng D được;
# clause geo ("extend") không phủ → escalate (KHÔNG silent-drop D, KHÔNG render sai).
Triangle ABC. On ray BA extended beyond A, take D.
# Escalate-safe: "beyond C" ≠ chữ thứ 2 của "ray BA" (A) → RAY_EXTENDED validation
# (Z===Y) fail → hướng không xác định → escalate (chống dựng sai hướng).
Triangle ABC. On ray BA extended beyond C, take D such that CD = 5.

# ── cevian EN (issue #46 nhóm B) ─────────────────────────────────────────────
# median/altitude = FIX SILENT-INCOMPLETE (trước render TRIANGLE-ONLY bỏ chân âm
# thầm vì "median"/"altitude" chưa vocab → văn xuôi; nay +vocab + EN pattern →
# render đủ chân + đoạn, win lộ ở DÒNG OPS). bisector = RENDER-WIN (escalate→DET:
# "bisector" đã vocab batch6 → clause geo unclaimed → escalate; nay cevian claim).
# DET render: "median AM" → midpoint M của BC + connect(A,M).
Triangle ABC. Draw the median AM.
# DET render: "altitude AH" → perpFoot H trên BC + connect(A,H).
Triangle ABC. Draw the altitude AH.
# DET render (RENDER-WIN flip): "angle bisector AD" → angleBisectorFoot D + connect(A,D).
Triangle ABC. Draw the angle bisector AD.
# DET render: reverse "AM is the median" → midpoint M + connect.
Triangle ABC. AM is the median.
# Escalate-safe: external bisector EN DEFER → internal guard (?<![Ee]xternal\s)
# reject → clause "bisector" geo unclaimed → escalate (KHÔNG nhận nhầm internal).
Triangle ABC. Draw the external bisector AD.
# Escalate-safe (regression guard double-emit): "Perpendicular bisector AD" HOA đầu
# clause → lookbehind (?<![Pp]erpendicular\s) chặn cevian → chỉ perpBisector claim
# (D chưa định nghĩa → transpile-fail) → KHÔNG double-emit silent-wrong.
Triangle ABC. Perpendicular bisector AD is the axis.

# ── connect EN (issue #46 nhóm B — mục CUỐI nhóm B) ──────────────────────────
# connect.ts vốn VN-only (tia/nối/đường thẳng/đoạn). Thêm EN segment/line/ray/
# Connect/Join/Draw. TÁC ĐỘNG = FIX SILENT-INCOMPLETE: trước render TRIANGLE-ONLY
# (clause connect EN là văn xuôi → âm thầm bỏ đoạn/đường/tia) → nay render đủ, win
# lộ ở DÒNG OPS (+connect/...). Parity vocab VN/EN: "đoạn"↔"segment" (vào vocab);
# "đường thẳng"/"tia"/"nối"/"kẻ"↔"line"/"ray"/"connect"/"join"/"draw" CỐ Ý KHÔNG vocab
# (render in-context, escalate standalone). VN byte-identical (connect VN-only token).
# DET (silent-incomplete fix): "Draw segment AB" → connect/segment AB.
Triangle ABC. Draw segment AB.
# DET: "Draw line AC" → connect/line AC (in-context, "line" không vocab).
Triangle ABC. Draw line AC.
# DET: "Draw ray AB" → connect/ray AB (hướng A→B đúng, không phải opposite ray).
Triangle ABC. Draw ray AB.
# DET: "Connect B and C" (1 ký tự + connector) → connect/segment BC.
Triangle ABC. Connect B and C.
# DET: "Join AB" (cặp HOA) → connect/segment AB.
Triangle ABC. Join AB.
# DET render-win THẬT (đoạn trung tuyến AM trước bị bỏ): midpoint M + connect/segment AM.
Triangle ABC. Let M be the midpoint of BC. Draw segment AM.
# DET render-win THẬT (đoạn trung bình MN): 2 midpoint + connect/segment MN.
Triangle ABC. M is the midpoint of AB. N is the midpoint of AC. Draw segment MN.
# GUARD silent-wrong (EN mirror TIA_DOI batch9): "opposite ray of ray BA" → điểm D
# nằm trên tia ĐỐI (pointAtDistance lo) → connect KHÔNG vẽ ray BA sai hướng. OPS
# PHẢI giống before/after (chỉ triangle + pointAtDistance, KHÔNG connect/ray).
Triangle ABC. On the opposite ray of ray BA, take D such that BD = AB.
# GUARD dedup: "Draw AH perpendicular to BC" khớp CẢ perpFoot (connect A,H,segment)
# lẫn DRAW_PAIR_EN (cũng connect A,H,segment) → JSON-dedup → ĐÚNG 1 connect/segment AH
# (KHÔNG double). OPS giống before/after (perpFoot đã emit connect từ batch10).
Triangle ABC. Draw AH perpendicular to BC at H.
# GUARD NOUN_OWNED: "perpendicular bisector line BC" → perpBisector sở hữu cặp →
# connect KHÔNG emit line BC trần (NOUN_OWNED_BEFORE_EN suppress). OPS chỉ
# connect/perpBisector BC, KHÔNG connect/line BC (giống before/after).
Triangle ABC. Draw the perpendicular bisector line BC.
# Escalate-safe: connect standalone không có điểm định nghĩa → coverage complete
# (segment vocab) NHƯNG transpile-fail (A,B chưa tồn tại) → escalate (fail-safe,
# KHÔNG render đoạn rác giữa 2 điểm vô định).
Draw segment AB.

# === ISSUE #47 construct 1: đường thẳng Euler ===
# RENDER-WIN (flip ESCALATE→DET sau khi implement): tam giác scalene → G,H,O phân
# biệt + thẳng hàng → lineThrough(G,H,O). OPS: draw-shape/triangle, add-point/
# centroid, add-point/orthocenter, add-point/circumcenter, draw-line/lineThrough.
Cho tam giác ABC. Vẽ đường thẳng Euler.
Cho tam giác ABC. Vẽ đường Euler của tam giác ABC.
# Escalate-safe SUY BIẾN: tam giác đều → G≡H≡O → đường Euler không xác định →
# rule TỪ CHỐI emit → clause "đường thẳng Euler" (geo, có vocab "euler") không
# được claim → coverage incomplete → ESCALATE (KHÔNG render đường rác qua điểm trùng).
Cho tam giác đều ABC. Vẽ đường thẳng Euler.
# Escalate-safe: "đường tròn Euler" = đường tròn chín điểm (nine-point, DEFER #47)
# — KHÔNG bị Euler-LINE rule claim (regex yêu cầu "thẳng"/"đường Euler", loại "tròn");
# vocab "euler" làm clause thành geo-clause → unclaimed → ESCALATE (fail-safe).
Cho tam giác ABC. Vẽ đường tròn Euler.

# === ISSUE #47 construct 2: trục đẳng phương (radical axis) ===
# RENDER-WIN: 2 đường tròn (O;3),(I;2) → trục ⊥ O I, qua điểm power bằng nhau.
# OPS: 2× draw-circle/centerRadius + draw-line/radicalAxis (circle-derived line).
Cho hai đường tròn (O; 3) và (I; 2). Vẽ trục đẳng phương của chúng.
Cho hai đường tròn (O; 4) và (I; 1). Vẽ trục đẳng phương của hai đường tròn.
# Escalate-safe SUY BIẾN: 2 đường tròn ĐỒNG TÂM (cùng tâm O) → |O₁O₂|=0 → trục
# không xác định. circleRadius addShape idempotent → 1 circle 'O'; rule thấy 1
# tâm phân biệt ≠ 2 → KHÔNG emit → clause "đẳng phương" (geo) unclaimed → ESCALATE.
Cho hai đường tròn đồng tâm (O; 3) và (O; 5). Vẽ trục đẳng phương.
# Escalate-safe: chỉ 1 đường tròn → cần đúng 2 → rule không emit → ESCALATE
# (clause "đẳng phương" có vocab → geo unclaimed → fail-safe, không render lung tung).
Cho đường tròn (O; 3). Vẽ trục đẳng phương.

# === ISSUE #47 construct 3: đường thẳng Simson ===
# RENDER-WIN: P trên đường tròn ngoại tiếp ABC → 3 chân ⊥ xuống BC/CA/AB đồng
# tuyến. Compose onCircle(P) + perpFoot×3 + lineThrough (tái dùng Euler). OPS:
# triangle + through3 + add-point/onCircle + 3× add-point/perpFoot + draw-line/lineThrough.
Cho tam giác ABC nội tiếp đường tròn (O). P là điểm trên (O). Vẽ đường thẳng Simson của P.
Cho tam giác ABC nội tiếp đường tròn (O). Vẽ đường thẳng Simson của điểm P.
# Escalate-safe: THIẾU đường tròn ngoại tiếp → không có (O) để đặt P → rule không
# emit → clause "Simson" (geo, vocab "simson") unclaimed → ESCALATE (fail-safe).
Cho tam giác ABC. Vẽ đường thẳng Simson của P.
# Escalate-safe: >1 tam giác (nhập nhằng dựng circumcircle nào) → ESCALATE.
Cho tam giác ABC nội tiếp (O) và tam giác DEF. Vẽ đường thẳng Simson của P.
