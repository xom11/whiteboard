// Stub — replaced in Task 7.
import type { HandlerCtx, TransformToolKey } from './ctx';

export function finalizeTransform(
  _ctx: HandlerCtx,
  _tool: TransformToolKey,
  _pendingIds: string[],
  _value: number,
): void {
  throw new Error('finalizeTransform stub — replaced in Task 7');
}
